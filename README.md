# example-native-project-with-nuget-dependencies

An example of how to consume native C++ NuGet dependencies.

This depends on a single NuGet package containing a static library written
in C++.  The library provides both debug and release versions for architectures
x86, x64, ARM, and ARM64.
